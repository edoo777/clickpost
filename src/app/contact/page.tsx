import { ContactView } from "@/app/contact/ContactView";

export const metadata = {
  title: "Contact — ClickPost",
  description: "Une question, un projet d'agence, ou envie d'en savoir plus sur ClickPost ? Écrivez-nous.",
};

export default function ContactPage() {
  return <ContactView />;
}
