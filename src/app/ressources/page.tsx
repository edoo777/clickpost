import { RessourcesView } from "@/app/ressources/RessourcesView";

export const metadata = {
  title: "Ressources — ClickPost",
  description: "Blog, guides et centre d'aide ClickPost pour construire une stratégie de contenu solide.",
};

export default function RessourcesPage() {
  return <RessourcesView />;
}
