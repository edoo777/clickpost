import { BlogIndexView } from "@/app/blog/BlogIndexView";

export const metadata = {
  title: "Blog — ClickPost",
  description: "Stratégie de contenu, réseaux sociaux, création de contenu, calendrier éditorial, intelligence artificielle, productivité et marketing.",
};

export default function BlogPage() {
  return <BlogIndexView />;
}
